export default function Index() {
  // This file serves as the entry point for the root "/" route.
  // The redirection logic in app/_layout.tsx will handle
  // pushing the user to /Home or /Login based on their auth state.
  return null;
}
